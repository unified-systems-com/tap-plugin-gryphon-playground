{
  "feature": "OR / NOT predicate combinators and parenthesized grouping in WHERE",
  "background": {
    "grift_fixture": "fixtures/sparse_dense.grift.json"
  },
  "scenarios": [
    {
      "name": "OR matches rows satisfying either branch",
      "tags": ["combinators", "or", "where"],
      "covers": [
        "req-grid-traversal-lang-combinators",
        "req-grid-traversal-lang-combinators-2"
      ],
      "inspired_by": "opencypher TCK — expressions/boolean (OR disjunction)",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE n.data.severity_score < 10 OR n.data.severity_score > 25 RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__or_branches.expected.json",
      "expected_sql_snapshot": "expected/combinators__or_branches.sql.txt"
    },
    {
      "name": "NOT negates a comparison",
      "tags": ["combinators", "not", "where"],
      "covers": [
        "req-grid-traversal-lang-combinators-3"
      ],
      "inspired_by": "opencypher TCK — expressions/boolean (NOT negation of a predicate)",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE NOT n.data.kind = \"island\" RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__not_comparison.expected.json",
      "expected_sql_snapshot": "expected/combinators__not_comparison.sql.txt"
    },
    {
      "name": "parenthesized grouping overrides AND/OR precedence",
      "tags": ["combinators", "grouping", "tck-inspired"],
      "covers": [
        "req-grid-traversal-lang-combinators-1",
        "req-grid-traversal-lang-combinators-4"
      ],
      "inspired_by": "opencypher TCK — expressions/boolean",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE n.data.kind = \"neighbor\" AND (n.data.severity_score < 15 OR n.data.severity_score > 25) RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__grouping.expected.json",
      "expected_sql_snapshot": "expected/combinators__grouping.sql.txt"
    },
    {
      "name": "NOT distributes over an AND (De Morgan)",
      "tags": ["combinators", "not", "tck-inspired"],
      "covers": [
        "req-grid-traversal-lang-combinators-1",
        "req-grid-traversal-lang-combinators-3"
      ],
      "inspired_by": "opencypher TCK — expressions/boolean",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE NOT (n.data.kind = \"neighbor\" AND n.data.is_open = true) RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__de_morgan.expected.json",
      "expected_sql_snapshot": "expected/combinators__de_morgan.sql.txt"
    },
    {
      "name": "OR composes with an IN-list",
      "tags": ["combinators", "or", "in-list"],
      "covers": [
        "req-grid-traversal-lang-combinators-2",
        "req-grid-traversal-lang-in"
      ],
      "inspired_by": "opencypher TCK — expressions/boolean (OR composed with IN membership)",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE n.data.kind IN [\"island\"] OR n.data.severity_score > 25 RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__or_with_in.expected.json",
      "expected_sql_snapshot": "expected/combinators__or_with_in.sql.txt"
    },
    {
      "name": "NOT in the aggregation path narrows before the COUNT",
      "tags": ["combinators", "not", "aggregation", "advanced"],
      "covers": [
        "req-grid-traversal-lang-combinators-3",
        "req-grid-gryphon-count"
      ],
      "inspired_by": "opencypher TCK — expressions/boolean (NOT filter applied before aggregation)",
      "layer": "lite",
      "query": "MATCH (h)-[:PG_LINKS__grid_fixtures]->(n:grid_fixtures__node) WHERE NOT n.data.kind = \"island\" RETURN h.entity_id AS src, COUNT(n) AS c",
      "expected_envelope": "expected/combinators__not_aggregation.expected.json",
      "expected_sql_snapshot": "expected/combinators__not_aggregation.sql.txt"
    },
    {
      "name": "De Morgan — NOT over a disjunction keeps the band between the bounds",
      "tags": ["combinators", "not", "or", "de-morgan"],
      "covers": ["req-grid-traversal-lang-combinators-2"],
      "inspired_by": "opencypher TCK — expressions/boolean (De Morgan over a disjunction: NOT (a OR b))",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE NOT (n.data.severity_score < 10 OR n.data.severity_score > 25) RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__not_over_or.expected.json",
      "expected_sql_snapshot": "expected/combinators__not_over_or.sql.txt"
    },
    {
      "name": "double negation NOT NOT collapses to the inner predicate",
      "tags": ["combinators", "not", "double-negation"],
      "covers": ["req-grid-traversal-lang-combinators-1"],
      "inspired_by": "opencypher TCK — expressions/boolean (double negation NOT NOT x)",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE NOT (NOT n.data.kind = \"island\") RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__double_negation.expected.json",
      "expected_sql_snapshot": "expected/combinators__double_negation.sql.txt"
    },
    {
      "name": "three chained AND operands narrow left-to-right",
      "tags": ["combinators", "and", "associativity"],
      "covers": ["req-grid-traversal-lang-combinators"],
      "inspired_by": "opencypher TCK — expressions/boolean (AND/OR associativity with 3+ chained operands)",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) WHERE n.data.kind = \"neighbor\" AND n.data.severity_score > 10 AND n.data.severity_score < 30 RETURN n.entity_id AS id ORDER BY id",
      "expected_envelope": "expected/combinators__three_and.expected.json",
      "expected_sql_snapshot": "expected/combinators__three_and.sql.txt"
    }
  ]
}
