{
  "feature": "NOT EXISTS anti-join excludes rows whose correlated subquery matches",
  "background": {
    "grift_fixture": "fixtures/optional_relations.grift.json"
  },
  "scenarios": [
    {
      "name": "NOT EXISTS excludes targets that have a guard edge",
      "tags": ["not-exists", "advanced", "current-behavior"],
      "covers": [
        "req-grid-gryphon-not-exists",
        "req-grid-gryphon-not-exists-3"
      ],
      "inspired_by": "opencypher TCK — clauses/match-where (NOT EXISTS negated pattern predicate)",
      "layer": "lite",
      "query": "MATCH (s)-[e:PG_LINKS__grid_fixtures]->(t) NOT EXISTS { MATCH (g)-[:PG_OPTIONAL__grid_fixtures]->(t) }",
      "expected_envelope": "expected/not_exists__unguarded_envelope.expected.json",
      "expected_sql_snapshot": "expected/not_exists__unguarded_envelope.sql.txt"
    },
    {
      "name": "NOT EXISTS combined with COUNT counts only unguarded targets",
      "tags": ["not-exists", "aggregation", "count", "current-behavior"],
      "covers": [
        "req-grid-gryphon-not-exists",
        "req-grid-gryphon-count",
        "req-grid-gryphon-rows"
      ],
      "inspired_by": "opencypher TCK — clauses/match-where (NOT EXISTS composed with COUNT)",
      "layer": "lite",
      "query": "MATCH (s)-[:PG_LINKS__grid_fixtures]->(t) NOT EXISTS { MATCH (g)-[:PG_OPTIONAL__grid_fixtures]->(t) } RETURN s.entity_id AS source_id, COUNT(t) AS unguarded_count",
      "expected_envelope": "expected/not_exists__unguarded_count.expected.json",
      "expected_sql_snapshot": "expected/not_exists__unguarded_count.sql.txt"
    },
    {
      "name": "NOT EXISTS inner WHERE narrows the anti-join so guarded target survives",
      "tags": ["not-exists", "advanced", "inner-where", "correlated"],
      "covers": [
        "req-grid-gryphon-not-exists",
        "req-grid-gryphon-not-exists-3"
      ],
      "inspired_by": "opencypher TCK — clauses/match-where (NOT EXISTS subquery carrying its own correlated inner WHERE)",
      "layer": "lite",
      "query": "MATCH (s)-[e:PG_LINKS__grid_fixtures]->(t) NOT EXISTS { MATCH (g:grid_fixtures__node)-[:PG_OPTIONAL__grid_fixtures]->(t) WHERE g.data.severity_score > 100 }",
      "expected_envelope": "expected/not_exists__inner_where_narrows.expected.json",
      "expected_sql_snapshot": "expected/not_exists__inner_where_narrows.sql.txt"
    },
    {
      "name": "NOT EXISTS over a multi-hop inner chain that matches nothing keeps every target",
      "tags": ["not-exists", "advanced", "multi-hop", "correlated"],
      "covers": [
        "req-grid-gryphon-not-exists",
        "req-grid-gryphon-not-exists-3"
      ],
      "inspired_by": "opencypher TCK — clauses/match-where (NOT EXISTS over a 2-hop inner pattern)",
      "layer": "lite",
      "query": "MATCH (s)-[e:PG_LINKS__grid_fixtures]->(t) NOT EXISTS { MATCH (a)-[:PG_LINKS__grid_fixtures]->(b)-[:PG_OPTIONAL__grid_fixtures]->(t) }",
      "expected_envelope": "expected/not_exists__multi_hop_inner.expected.json",
      "expected_sql_snapshot": "expected/not_exists__multi_hop_inner.sql.txt"
    }
  ]
}
