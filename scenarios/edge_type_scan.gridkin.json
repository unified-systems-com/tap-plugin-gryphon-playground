{
  "feature": "Edge-type-scan dispatch returns all edges of a type between typed endpoints",
  "background": {
    "grift_fixture": "fixtures/sparse_dense.grift.json"
  },
  "scenarios": [
    {
      "name": "PG_LINKS edges from pg_hub to pg_node",
      "tags": ["edge-type-scan", "dispatch", "current-behavior"],
      "covers": [
        "req-grid-traversal-lang-patterns",
        "req-grid-traversal-exec-pipeline"
      ],
      "inspired_by": "opencypher TCK — clauses/match (typed relationship pattern, directed endpoints)",
      "layer": "lite",
      "query": "MATCH (a:grid_fixtures__hub)-[e:PG_LINKS__grid_fixtures]->(b:grid_fixtures__node)",
      "expected_envelope": "expected/edge_type_scan__hub_to_node.expected.json",
      "expected_sql_snapshot": "expected/edge_type_scan__hub_to_node.sql.txt"
    },
    {
      "name": "PG_LINKS edges between pg_node endpoints",
      "tags": ["edge-type-scan", "dispatch", "current-behavior"],
      "covers": ["req-grid-traversal-lang-patterns"],
      "inspired_by": "opencypher TCK — clauses/match (typed relationship between same-label endpoints)",
      "layer": "lite",
      "query": "MATCH (a:grid_fixtures__node)-[e:PG_LINKS__grid_fixtures]->(b:grid_fixtures__node)",
      "expected_envelope": "expected/edge_type_scan__node_to_node.expected.json",
      "expected_sql_snapshot": "expected/edge_type_scan__node_to_node.sql.txt"
    },
    {
      "name": "incoming-direction match yields the same edges read from the target side",
      "tags": ["edge-type-scan", "dispatch", "direction", "current-behavior"],
      "covers": ["req-grid-traversal-lang-patterns"],
      "inspired_by": "opencypher TCK — clauses/match (incoming-direction relationship read from the target endpoint)",
      "layer": "lite",
      "query": "MATCH (b:grid_fixtures__node)<-[e:PG_LINKS__grid_fixtures]-(a:grid_fixtures__hub)",
      "expected_envelope": "expected/edge_type_scan__incoming.expected.json",
      "expected_sql_snapshot": "expected/edge_type_scan__incoming.sql.txt"
    },
    {
      "name": "typed pattern against an absent edge type returns an empty envelope",
      "tags": ["edge-type-scan", "dispatch", "empty", "current-behavior"],
      "covers": ["req-grid-traversal-lang-patterns"],
      "inspired_by": "opencypher TCK — clauses/match (typed relationship pattern matching no edges yields empty result)",
      "layer": "lite",
      "query": "MATCH (a:grid_fixtures__hub)-[e:PG_OWNS]->(b:grid_fixtures__node)",
      "expected_envelope": "expected/edge_type_scan__absent_type_empty.expected.json",
      "expected_sql_snapshot": "expected/edge_type_scan__absent_type_empty.sql.txt"
    },
    {
      "name": "envelope with a non-anchor WHERE over two bound nodes filters both endpoints",
      "tags": ["edge-type-scan", "where", "envelope-where-defect"],
      "covers": ["req-grid-traversal-lang-filters", "req-grid-traversal-lang-combinators"],
      "inspired_by": "opencypher TCK — clauses/match-where (multi-variable conjunction constraining two different bound nodes)",
      "layer": "lite",
      "query": "MATCH (h:grid_fixtures__hub)-[:PG_LINKS__grid_fixtures]->(n:grid_fixtures__node) WHERE h.data.severity_score = 0 AND n.data.severity_score > 15",
      "expected_envelope": "expected/edge_type_scan__two_node_where.expected.json",
      "expected_sql_snapshot": "expected/edge_type_scan__two_node_where.sql.txt"
    }
  ]
}
