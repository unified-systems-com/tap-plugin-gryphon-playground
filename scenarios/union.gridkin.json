{
  "feature": "Multiple MATCH clauses union into one deduplicated graph envelope",
  "background": {
    "grift_fixture": "fixtures/sparse_dense.grift.json"
  },
  "scenarios": [
    {
      "name": "two type-scan MATCH clauses union pg_hub and pg_node nodes",
      "tags": ["union", "multi-match", "current-behavior"],
      "covers": ["req-grid-traversal-lang-shape-5"],
      "inspired_by": "opencypher TCK — clauses/union (UNION of two label scans, deduplicated)",
      "layer": "lite",
      "query": "MATCH (a:grid_fixtures__hub) MATCH (b:grid_fixtures__node)",
      "expected_envelope": "expected/union__hub_and_node.expected.json",
      "expected_sql_snapshot": "expected/union__hub_and_node.sql.txt"
    },
    {
      "name": "overlapping arms dedup — nodes matching both bounds appear once",
      "tags": ["union", "multi-match", "dedup", "tck-inspired"],
      "covers": ["req-grid-traversal-lang-shape-5"],
      "inspired_by": "opencypher TCK — clauses/union (overlapping arms force deduplication)",
      "layer": "lite",
      "query": "MATCH (n:grid_fixtures__node) MATCH (m:grid_fixtures__node) WHERE n.data.severity_score >= 15 AND m.data.severity_score <= 20",
      "expected_envelope": "expected/union__overlap_dedup.expected.json",
      "expected_sql_snapshot": "expected/union__overlap_dedup.sql.txt"
    },
    {
      "name": "a NOT predicate scoped to one arm's variable leaves the other arm unfiltered",
      "tags": ["union", "multi-match", "combinators", "not", "scoping", "regression"],
      "covers": ["req-grid-traversal-lang-shape-5"],
      "inspired_by": "opencypher TCK — no applicable feature folder (property-fuzzer regression, req-gridkin-property-fuzz: per-clause WHERE pruning must drop an unbound-variable leaf, not evaluate it under NOT)",
      "layer": "lite",
      "query": "MATCH (a:grid_fixtures__node) MATCH (b:grid_fixtures__node) WHERE NOT (a.data.kind IN [\"island\"])",
      "expected_envelope": "expected/union__not_scoped_to_one_arm.expected.json",
      "expected_sql_snapshot": "expected/union__not_scoped_to_one_arm.sql.txt"
    }
  ]
}
