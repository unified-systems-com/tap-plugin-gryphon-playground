{
  "version": 1,
  "description": "Gryphon soak \u2014 the fuzz-campaign flavor. Same install as the playground record (grid_fixtures + gryphon_playground, public git-sourced), but RESERVED for the differential-fuzz soak: a long unattended campaign that grinds fresh seed bands looking for executor/oracle divergences. IMPORTANT (honest scope, not a latent guarantee): the fuzz campaign is driven OUT-OF-BAND today by scripts/gryphon-fuzz-campaign against a standing instance; it is NOT yet a runtime boot-population task loop. This record exists now to exercise multi-record selection (`<source-ref>#soak` vs `#playground`) and the manifest\u2194files coherence guard; its population steps are wired to the campaign task loop later, when that task surface exists (tracked, not implied here). Until then, boot `#soak`, then run scripts/gryphon-fuzz-campaign against it. Selected by pointer `<source-ref>#soak`.",
  "install": {
    "plugins": [
      {
        "slug": "grid_fixtures",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-grid-fixtures",
          "rev": "v0.3.1"
        },
        "note": "Package-mode (tap_plugin.grid_fixtures). Install-only: the neutral grid_fixtures__* node/edge vocabulary the Gridkin corpus references by string. Migrated so the corpus has real types to query. No GRIFT seed, no cross-plugin deps. Listed before gryphon_playground, which depends_on it. Pulled at boot from its own PUBLIC repo \u2014 a public git source declares no `credential` (req-tap-plugin-arch-source-secret-5); rev pinned to the immutable tag v0.3.1."
      },
      {
        "slug": "gryphon_playground",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-gryphon-playground",
          "rev": "v0.3.2"
        },
        "note": "Self-reference (app-of-apps, req-boot-bootstrap-stage0-3). Same install as the playground record; the flavor difference is in intended behavior (fuzz soak), reserved above. Pulled at boot from its own PUBLIC repo \u2014 a public git source declares no `credential` (req-tap-plugin-arch-source-secret-5); rev pinned to the immutable tag v0.3.2."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": []
  }
}
