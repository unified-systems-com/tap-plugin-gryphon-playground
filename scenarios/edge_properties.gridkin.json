{
  "feature": "Inline edge-property filter narrows an edge-type scan by edge properties",
  "background": {
    "grift_fixture": "fixtures/edge_properties.grift.json"
  },
  "scenarios": [
    {
      "name": "inline edge-property map filters PG_LINKS edges by tier",
      "tags": ["edge-type-scan", "inline-property", "current-behavior"],
      "covers": ["req-grid-traversal-lang-filters-1"],
      "inspired_by": "opencypher TCK — clauses/match (inline relationship-property predicate)",
      "layer": "lite",
      "query": "MATCH (a:grid_fixtures__node)-[e:PG_LINKS__grid_fixtures {tier: \"gold\"}]->(b:grid_fixtures__node)",
      "expected_envelope": "expected/edge_properties__tier_gold.expected.json",
      "expected_sql_snapshot": "expected/edge_properties__tier_gold.sql.txt"
    }
  ]
}
